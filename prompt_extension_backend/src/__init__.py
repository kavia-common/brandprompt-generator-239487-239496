"""
Backend source package for the prompt extension.

Keeping src as an explicit Python package ensures imports like `from src.api.main import app`
and new modules under `src.*` resolve consistently in different execution contexts.
"""
